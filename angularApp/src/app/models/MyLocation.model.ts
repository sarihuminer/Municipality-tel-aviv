export class MyLocation {
    latitude: string;
    longitude: string;
    timestamp: string;
    note: string;
    constructor(latitude: string,
        longitude: string,
        timestamp: string,
        note: string) {

    }
}